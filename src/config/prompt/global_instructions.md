Based on the following context from the codebase, please answer the question.

**Code Attribution**: When showing existing code examples from the codebase, always prefix each code block with a comment indicating the source file path (e.g., `// From: embedding-name/path/to/file.tsx`).
