"""
Providers module for Context-AI.

Clean, explicit imports only. No re-exports or aggregations.
Use direct imports from specific provider modules.

Examples:
    from config.providers.claude import CLAUDE_MODELS, DEFAULT_MODEL
    from config.providers.protocols import AIProviderProtocol
    from config.providers.models import ProviderCapabilities
    from config.providers.registry import get_provider_registry
"""
